pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal() // This allows resolving Gradle plugins
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "Sundial"
include ':app'
